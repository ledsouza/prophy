"use client";

export default function UnitPage() {
    // This component doesn't render anything as content comes from parallel routes
    // The layout.tsx will render the appropriate parallel route based on user role
    return null;
}
